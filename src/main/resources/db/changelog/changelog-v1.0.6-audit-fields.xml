<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="6" author="system">
        <comment>Adicionar campos de auditoria na tabela ordem_servico</comment>

        <addColumn tableName="ordem_servico">
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
            <column name="deleted_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Atualizar registros existentes com a data_inicio -->
        <sql>
            UPDATE ordem_servico
            SET created_at = data_inicio,
                updated_at = data_inicio
            WHERE created_at IS NULL;
        </sql>
    </changeSet>

    <changeSet id="7" author="system">
        <comment>Adicionar campos de auditoria na tabela cliente</comment>

        <addColumn tableName="cliente">
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
            <column name="deleted_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Atualizar registros existentes com a data atual -->
        <sql>
            UPDATE cliente
            SET created_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
            WHERE created_at IS NULL;
        </sql>
    </changeSet>

</databaseChangeLog>
